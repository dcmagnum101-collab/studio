rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For a private app, allow any authenticated user full access.
    // This is the most robust way to ensure the single user always has permission.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
